/**
 * VersionsPlugin - Stub plugin for collaboration version management
 * This plugin is only used when collaboration features are enabled
 */

export function VersionsPlugin({id}: {id: string}): null {
  // This plugin is a stub for collaboration version management
  // It doesn't render anything but is required for the collaboration feature
  return null;
}

